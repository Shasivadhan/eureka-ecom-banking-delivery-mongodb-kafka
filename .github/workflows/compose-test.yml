name: Docker Compose Integration Test

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  integration-test:
    runs-on: ubuntu-latest

    services:
      docker:
        image: docker:26.1.3-dind
        privileged: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build all microservices
        run: |
          mvn -B -f eureka-server/pom.xml clean package -DskipTests
          mvn -B -f ecommerce/pom.xml clean package -DskipTests
          mvn -B -f banking/pom.xml clean package -DskipTests
          mvn -B -f delivery/pom.xml clean package -DskipTests

      - name: Start services with Docker Compose
        run: |
          docker compose -f docker-compose.yml up -d --build
          docker ps

      - name: Wait for Eureka to be healthy
        run: |
          echo "Waiting for Eureka..."
          for i in {1..30}; do
            if curl -s http://localhost:8761 | grep -q "Eureka"; then
              echo "✅ Eureka is up!"
              exit 0
            fi
            sleep 5
          done
          echo "❌ Eureka did not start in time"
          docker logs $(docker ps -q)
          exit 1

      - name: Run API smoke tests
        run: |
          echo "Checking Ecommerce API..."
          curl -f http://localhost:8081/actuator/health || exit 1
          echo "Checking Banking API..."
          curl -f http://localhost:8082/actuator/health || exit 1
          echo "Checking Delivery API..."
          curl -f http://localhost:8083/actuator/health || exit 1

      - name: Stop services
        if: always()
        run: docker compose -f docker-compose.yml down
